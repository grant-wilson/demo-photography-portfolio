import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  base: "/demo-photography-portfolio/",
  integrations: [],
  devToolbar: {
    enabled: false,
  },
});
